import { styled } from './stitches.config';

const Nav = styled('nav', {
    display: 'flex',
    justifyContent: 'space-between',
    padding: '20px',
    margin: '0',
    alignItems: 'center',
})

export default Nav;